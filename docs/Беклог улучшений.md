# Беклог улучшений

> Находки UX-ревью и сверки с данными аналитика, **отложенные осознанно**. Это детали/фидельность — НЕ уровень вижна. Брать только при переходе к боевой реализации, не сейчас. Решение Дениса 2026-06-01: для вижн-прототипа делаем только заявко-центричность менеджера, остальное паркуем.

## Сделано (вижн-уровень)
- [x] Заявко-центричный менеджер: очередь группируется по заявке + «путь заявки» (Steps OBO→KYC→VKYC) в карточке разбора.

## Отложено — детали/полировка (из UX-ревью)
- [ ] **Терминология RM:** панель решения зовётся по-разному («Решение по задаче/алерту», «Вердикт», «Действие»); объект то «Задача», то «Алерт» → унифицировать.
- [ ] **Чип роли в шапке** захардкожен «DVU · Проверка OBO» — висит на KYC/VKYC экранах → подставлять домен.
- [ ] **/confirm теряет прогресс:** «Документы 0 из 3», «VKYC 0 из 2» даже после прохождения → прокинуть mock-состояние.
- [ ] **Нет валидации/обязательности** (анкета, документы пропускают пустыми) — отключено осознанно для прохода; вернуть на этапе тестирования.
- [ ] **Жаргон для клиента** (VCIP/eKyc/self-VKYC/F2F без расшифровки) → расшифровывать при первой встрече, говорить «видеоидентификация».
- [ ] **Приоритеты High/Medium почти одного цвета** (оба оранжевые) → развести тона.
- [ ] **/rm/session:** пустой Select слота, «мёртвая» кнопка без подсказки.
- [ ] **/vcip-session:** внутренний Steps не докрашивает последний шаг при успехе.
- [ ] **/business:** индустрия без значения по умолчанию (есть на /company).
- [ ] **/rm/task DVU-1042:** «сверить подписи», а документы «не загружены» — нечего сверять → данные под причину.

## Отложено — расхождения с данными аналитика (doc 03)
- [ ] **Correspondence address компании** — в спеке ручное поле «если отличается от Registered», на CL-03 нет (в типе есть).
- [ ] **Industry дубль:** показываем на CL-03 и CL-04; в спеке это BNQ-атрибут. + противоречие `Mock-модель` (поле Company) vs `doc 03` (BNQ) — решить, где живёт.
- [ ] Correspondence address подписанта (VCIP-секция), фото PAN-карты во время VCIP — мелочи.
- [ ] Residency/PEP — аналитик формулирует по каждому подписанту, у нас на уровне компании.

## За пределами карты (кандидаты)
- [ ] Миграция RM на Caldera (свой контракт компонентов).
- [ ] Под-роли менеджера (юр.проверка / KYC / операционка) через RBAC.
- [ ] Типы юрлиц помимо Private Limited; не-STP кейсы в mock-модели.
- [ ] Разделение подписантов-актёров (приглашение отправлено + кто прошёл/не прошёл).
