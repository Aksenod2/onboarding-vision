# Комплаенс-чеклист — клиентские экраны

> Живой документ: что НЕЛЬЗЯ допускать на клиентских экранах. Владелец — Эля (проверяет тексты), поведенческие пункты сверяет Вера при QA-проходах, новые правила приносит Боря из BRD/решений.
> Формат: правило → откуда → как проверять.

## Тексты (зона Эли)

- **Не называть клиенту внутренние источники данных: Probe42, PAN-база/NSDL, OFAC, Stop-42.** Допустимо: «по нашим данным», «мы определили»; CKYC раскрывать МОЖНО. — Задача 10 (комплаенс-правило формулировок), подтверждено BRD. Проверка: grep по словарям экранов + чтение текстов.
- **Риск-категорию/риск-скор клиенту не показывать нигде** (ни балл, ни категорию Low/Medium/High). — Решение Марго 2026-06-08 (Р-2); `risk.shownToClient: false` в mock. Проверка: на экранах нет слов risk/риск в клиентском значении.
- **Тексты согласий, деклараций и дисклеймеров — строго verbatim из документов банка.** Не перефразировать, не «улучшать», даже если есть ошибка — ошибку отмечать как вопрос к Марго. — Правило Марго (verbatim-согласия). Проверка: сверка с `docs/Consents — список (current).md` и BRD.

## Поведение / данные (зона Веры)

- **Фото/изображение Aadhaar не хранить, номер Aadhaar маскировать (redact).** — BRD: `2-VCIP → V-CIP Procedure п.7`; CISO MoM «Aadhaar card фото в VCIP не хранить». Проверка: на экранах и в mock не светится полный номер Aadhaar.
- **Прерванную видеосессию нельзя «продолжить» — только новая сессия целиком.** — BRD: `2-VCIP → V-CIP Procedure п.2` (RBI). Проверка: на экране видеоидентификации нет «возобновить»; «Назад» отсутствует.

## Журнал проверок

_(дата · кто · экраны · результат — пополняется при прогонах)_

- 2026-06-10 · Вера (QA) · полный поток v2 SP01→SP10 (браузерный прогон, Playwright) · Поведение: Aadhaar не светится ✅, «возобновить»/«Назад» на видео нет ✅. Тексты (зона Эли, замечено): согласие на шаге 1 называет «Income Tax PAN database / PAN-база Налогового департамента» (чек-лист запрещает называть PAN-базу; согласие verbatim — вопрос к Марго); тост «forwarded for DVU review» после Q5 PEP=Yes виден клиенту (спека Задачи 10: фоновые триггеры не показываем); v1-лендинг на корне `/` называет Probe42 клиенту. Полный отчёт — в чате прогона.
